.credentials*
cache
debug
file-history
history.jsonl
ide
local
plans
projects
settings.json
shell-snapshots
stats-cache.json
statsig
telemetry
todos
tmp.credentials.json

# Runtime state / caches
sessions/
tasks/
usage-data/
backups/
chrome/
mcp-needs-auth-cache.json
plugins/blocklist.json
plugins/install-counts-cache.json
plugins/plugin-catalog-cache.json
plugins/known_marketplaces.json
plugins/marketplaces/

# Nested claude install artifacts
.claude/

# Local-only secrets and per-host state
token
.claude.json*
downloads
session-env

# Runtime state / caches (cleanup 2026-07-18) — regenerated by Claude Code
.last-cleanup
.last-update-result.json
paste-cache/
uploads/
plugins/.last_inuse_sweep
plugins/installed_plugins.json
policy-limits.json
remote-settings.json
scheduled_tasks.lock
settings.local.json

# agent daemon runtime state (2026-09-01) — control keys, dispatch, per-job scratch
daemon/
daemon.lock
daemon.log
daemon.status.json
jobs/

# local session records — not committed (also keeps internal logs out of the public repo)
session-logs/

# release build output — regenerate with scripts/release/make-release.sh
scripts/release/out/

# macOS
.DS_Store
